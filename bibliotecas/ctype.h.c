#include <ctype.h>

/*
 * isalnum()-> verifica se o caractere é alfanumerico(abc123)
 * isalpha(c) -> Verifica se o caractere c é uma letra do alfabeto
 * isdigit(c) -> Verifica se o caractere c é um dígito numérico
 * isalnum(c) -> Verifica se o caractere c é alfanumérico (letra ou dígito)
 * isupper(c) -> Verifica se o caractere c é uma letra maiúscula
 * islower(c) -> Verifica se o caractere c é uma letra minúscula
 * isspace(c) -> Verifica se o caractere c é um espaço em branco
 * isprint(c) -> Verifica se o caractere c é imprimível
 * ispunct(c) -> Verifica se o caractere c é um caractere de pontuação
 * isxdigit(c) -> Verifica se o caractere c é um dígito hexadecimal
 * isupper(c) -> verifica se é maiuscula
 * isspace(c) -> verifica se é um espaco
 *
 * Funções de Conversão de Caracteres
 *
 * tolower(c) -> Converte o caractere c para minúsculo, se for uma letra
 * toupper(c) -> Converte o caractere c para maiúsculo, se for uma letra
 */
